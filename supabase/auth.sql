-- Auth profiles + account RPCs + authenticated-only RLS
-- Applied via MCP; kept for docs/replay

-- (see migration add_profiles_auth_and_account_rpcs)
-- Initial admin: username admin / password admin  (email admin@store.local)
